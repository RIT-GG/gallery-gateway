query PortfolioPeriods($activeSubmission: Boolean) {
  portfolioPeriods(activeSubmission: $activeSubmission) {
    name
    description
    id
    startDate
    endDate
    judgingStartDate
    judgingEndDate
    createdAt
  }
}