query Portfolio($id: ID!) {
  portfolio(id: $id) {
    id
    title
    student_id
    student_name
    entries {
      id
      group{
        id
      }
      student {
        username
      }
    }
  }
}
